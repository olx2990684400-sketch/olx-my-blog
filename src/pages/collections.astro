---
import PageTitle from "@/components/common/PageTitle.astro";
import CollectionsEditor from "@/components/edit/CollectionsEditor.svelte";
import { collectionsApiConfig, siteConfig } from "@/config";
import I18nKey from "@/i18n/i18nKey";
import { i18n } from "@/i18n/translation";
import MainGridLayout from "@/layouts/MainGridLayout.astro";

if (!siteConfig.pages.collections) {
	return Astro.redirect("/404/");
}

const title = collectionsApiConfig.title || i18n(I18nKey.collections);
const description =
	collectionsApiConfig.description || i18n(I18nKey.collectionsDescription);

// 序列化初始数据传递给 Svelte 组件
const initialCollectionsJson = JSON.stringify(collectionsApiConfig);
---

<MainGridLayout title={title} description={description}>

  <div class="flex w-full rounded-(--radius-large) overflow-hidden relative min-h-32">
    <div class="card-base z-10 px-9 py-6 relative w-full">
      <!-- 页面标题 -->
      <div class="mb-8">
        <PageTitle title={title} eyebrow="Collections" variant="stacked" />
        {description && (
          <p class="text-base text-neutral-500 dark:text-neutral-400 leading-relaxed mt-3">
            {description}
          </p>
        )}
      </div>

      <!-- 收藏编辑器（Svelte 组件，包含编辑按钮、Tab、卡片渲染，仅客户端渲染） -->
      <div data-no-swup>
        <CollectionsEditor client:only initialData={initialCollectionsJson} />
      </div>
    </div>
  </div>
</MainGridLayout>
